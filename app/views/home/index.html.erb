<% if user_signed_in? %>
  <div> Welcome <%= current_user.email %> </div>
  <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>

      <%= link_to 'New Board', new_board_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded mt-4 block w-max" %>

<div class="grid grid-cols-4 gap-4 pt-12">
<% @boards.each do |board| %>
  <span class="block bg-blue-200 border py-8 px-4"><%= board.name %></span>
<% end %>
</div>

<% else %>
<div class="flex h-full justify-center flex-col md:flex-row w-full">
  <div class="w-full bg-white h-full flex items-center justify-center">
    <div class="p-12 flex flex-col gap-6">
    <h1 class="font-bold text-5xl">Welcome to Rails app</h1>
    <%= link_to "Create Account", new_user_registration_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded w-full" %>
      <%= link_to "Sign in", new_user_session_path,class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded w-full" %>
  </div>
  </div>
  <div class="w-full bg-blue-500 h-full"></div>
<div>


<% end %>


